<% @hermestitle = "Feeds" %>


<% if @feed_subscriptions.length > 0 %>

<p><b>Hermes feeds:</b></p>

<table class="defaultTable">
<tr>
<th>Feed</th>
<th>Formats</th>
</tr>

<% @feed_subscriptions.each  { |feed| %>
<tr>
<td><%= feed.subscription_desc %></td> 
<td>
<% if (feed.starship_messages.count > 0) %>
  <%= link_to 'web', {:action => "show", :controller => "feeds", :id => feed.id} %>
  <%= link_to 'rss', {:action => "show", :controller => "feeds", :id => feed.id, :format => 'rdf'} %>
  <%= link_to image_tag("feeds.png"), {:action => "show", :controller => "feeds", :id => feed.id, :format => 'rdf'} %>
<% end %>
</td>
</tr> 
<% } %>
</table>

<br/><p>
<b>Special feeds:</b></p>
<p>
- Multiple feeds merged to one: /feeds/&lt;id&gt;,&lt;id&gt;,&lt;id&gt; {.rdf|.html}
<br/>
- You can also create <%= link_to 'personal feeds', {:action => "personal", :controller => "feeds"} %> based on your own needs.  

</p>

<% else %>

<br/><p><i>There are currently no public hermes feeds configured. :-( </i></p>


<% end %>


<br/><p>
<b>Other openSUSE related feeds:</b></p>

<table class="defaultTable">
<tr>
<th>Feed</th>
<th>Formats</th>
</tr>

<tr><td>openSUSE News</td>
<td><%= link_to image_tag("feeds.png"), 'http://news.opensuse.org/feed/' %></td></tr>

<tr><td>Weekly News</td>
<td><%= link_to image_tag("feeds.png"), 'http://news.opensuse.org/category/weekly-news/feed/' %></td></tr>

<tr><td>opensuse-buildservice Mailinglist</td>
<td><%= link_to image_tag("feeds.png"), 'http://lists.opensuse.org/opensuse-buildservice/mailinglist.rss' %></td></tr>

<tr><td>Recent wiki changes</td>
<td><%= link_to image_tag("feeds.png"), 'http://en.opensuse.org/index.php?title=Special:RecentChanges&feed=rss' %></td></tr>

<tr><td>Planet SUSE Blogs</td>
<td><%= link_to image_tag("feeds.png"), 'http://planet.opensu.se/rss20.xml' %></td></tr>

</table>


